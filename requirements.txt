# Core framework
Django>=4.2,<5.0

# Document parsing
PyPDF2>=3.0.1
python-docx>=0.8.11

# AI / LLM access (Google Gemini client)
google-generativeai>=0.3.2

# API development (if you expose REST endpoints)
djangorestframework>=3.14.0
requests>=2.31.0

# Deployment helpers (optional but common)
gunicorn>=21.2.0            # WSGI server for production
whitenoise>=6.6.0           # static-file serving
django-cors-headers>=4.3.1  # enable CORS

# Environment management
python-dotenv>=1.0.0

# For postgreSQL connection parsing that we took from supabase
dj-database-url
psycopg2-binary
